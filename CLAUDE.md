# CLAUDE.md — KeystoneGhost (public staging repo)

Session memory parked here while Fredrik's global memory home is not mounted
(remote sessions are ephemeral). Listed in `.pkgmeta` ignore, so it never ships
in the addon zip. Sweep the section below into global memory from a session
where the memory home is connected, then clear it.

## ↑ Promote to global

- 2026-08-02 — merge into the "your AI is showing" knowledge
  (memory/context/, wherever that note lives): launch-tell hierarchy observed
  live on Paka Key InTime's r/wowaddons launch — the "100% AI slop" checklist
  ran icon → UI → code; the post's prose was never mentioned. Visual assets are
  the first-glance surfaces: an obviously generated logo/icon is the #1 tell,
  neon-dashboard UI styling second. Counter: human-made or commissioned icon,
  and lead announcements with a short clip of the real thing running instead of
  styled stills.
